{% extends "layouts/forms-website.njk" %}
{% from "masthead/macro.njk" import appMasthead %}

{% set description %}
If you're taking part in private beta, ask for support by sending a message to the <a href="https://teams.microsoft.com/l/channel/19:sLNy297i1n0fiPYsGJb3UoOITYURp7PtMx8iFwEfJWs1%40thread.tacv2/General?groupId=346d8ebd-c7b8-47d3-b178-e6a9b31c8fc2&amp;tenantId=770a2450-0227-4c62-90c7-4e38537f1102" class="govuk-link govuk-link--inverse govuk-link--no-visited-state">Microsoft Teams channel</a> or <a href="https://defra-digital-team.slack.com/archives/C080WP62PJP" class="govuk-link govuk-link--inverse govuk-link--no-visited-state">Defra Slack channel</a>.
{% endset -%}

{% block beforeContent %}
{{ appMasthead(masthead) }}
{% endblock -%}

{% block content %}
<div>
  <div class="govuk-width-container">
    <div class="govuk-grid-row">
      <div class="govuk-grid-column-two-thirds">
        {% for month in latest %}
          <table class="govuk-table">
          <caption class="govuk-table__caption govuk-table__caption--l">{{ month.when }}</caption>
          <thead class="govuk-table__head">
          <tr class="govuk-table__row">
            <th scope="col" class="govuk-table__header govuk-!-width-one-quarter">Product</th>
            <th scope="col" class="govuk-table__header govuk-!-width-three-quarters">Update</th>
          </tr>
          </thead>
          <tbody class="govuk-table__body">
          {% for update in month.updates %}
            <tr class="govuk-table__row">
            <td class="govuk-table__cell">
              {% if update.product.length === 1 %}
                {{ update.product[0] }}
              {% else %}
                <ul class="govuk-list">
                  {% for prod in update.product %}
                    <li>{{ prod }}</li>
                  {% endfor %}
                </ul>
              {% endif %}
            </td>
            <td class="govuk-table__cell collapse-first-p-margin">{{ update.content | markdown({ baseUrl: appBaseUrl }) | safe }}</td>
          </tr>
          {% endfor %}
          </tbody>
        </table>
        {% endfor %}
      </div>
    </div>
  </div>
</div>
{% endblock %}
